// valueOf()

// 1. The valueOf() method in JavaScript is used to return the primitive value of an object. 
// 2. It is primarily defined on built-in objects, such as Number, String, Boolean, Date, and Object.
// 3. String: When called on a string, valueOf() returns the string itself.
// also you have to pass array or objet valueof() return the array and object itself
// 4. valueOf() method is used to return the primitive value of an object,

let str = "Hello, World!";
console.log(str.valueOf()); // Outputs: "Hello, World!"
console.log(typeof str.valueOf()); // Outputs: "string"
